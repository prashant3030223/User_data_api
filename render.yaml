services:
  - type: web
    name: user-data-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: MASTER_AUTH_KEY
        sync: false # User will set this in Render dashboard
