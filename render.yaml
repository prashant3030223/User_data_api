services:
  - type: web
    name: user-data-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MASTER_AUTH_KEY
        sync: false # User will set this in Render dashboard
